@page
@model PoliclinicaHope.Pages.DepartamenteProceduri.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>DepartamentProcedura</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="DepartamentProcedura.ID" />
            <div class="form-group">
                <label asp-for="DepartamentProcedura.ProceduraId" class="control-label"></label>
                <select asp-for="DepartamentProcedura.ProceduraId" class="form-control" asp-items="ViewBag.ProceduraId"></select>
                <span asp-validation-for="DepartamentProcedura.ProceduraId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="DepartamentProcedura.DepartamentId" class="control-label"></label>
                <select asp-for="DepartamentProcedura.DepartamentId" class="form-control" asp-items="ViewBag.DepartamentId"></select>
                <span asp-validation-for="DepartamentProcedura.DepartamentId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
