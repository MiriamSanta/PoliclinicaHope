@page
@model PoliclinicaHope.Pages.DepartamenteProceduri.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DepartamentProcedura[0].Procedura)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DepartamentProcedura[0].Departament)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.DepartamentProcedura) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Procedura.ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Departament.ID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
