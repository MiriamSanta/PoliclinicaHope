@page
@model PoliclinicaHope.Pages.DepartamenteProceduri.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>DepartamentProcedura</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="DepartamentProcedura.ProceduraId" class="control-label"></label>
                <select asp-for="DepartamentProcedura.ProceduraId" class ="form-control" asp-items="ViewBag.ProceduraId"></select>
            </div>
            <div class="form-group">
                <label asp-for="DepartamentProcedura.DepartamentId" class="control-label"></label>
                <select asp-for="DepartamentProcedura.DepartamentId" class ="form-control" asp-items="ViewBag.DepartamentId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
